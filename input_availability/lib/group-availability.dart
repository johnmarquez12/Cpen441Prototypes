import 'package:flutter/material.dart';

class GroupAvailability extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Group Availability'),
      ),
      body: Center(
        child: Text('Group Availability'),
      ),
    );
  }
}
